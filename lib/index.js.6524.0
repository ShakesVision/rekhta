const $d832f2ef8a5ce6ac$export$4c3438cd8db02a1e = "https://api.theothermeunfolded.com";
const $d832f2ef8a5ce6ac$var$PAGE_KEY_ENDPOINT = "https://ebooksapi.rekhta.org/api_getebookpagebyid_websiteapp/?wref=from-site&&pgid=";
const $d832f2ef8a5ce6ac$var$DEFAULT_TILE_SIZE = 50;
const $d832f2ef8a5ce6ac$var$DEFAULT_TILE_GAP = 16;
const $d832f2ef8a5ce6ac$var$memoryJsonCache = new Map();
function $d832f2ef8a5ce6ac$export$b79684660ffced15(options = {}) {
    const backendBaseUrl = (options.backendBaseUrl || $d832f2ef8a5ce6ac$export$4c3438cd8db02a1e).replace(/\/$/, "");
    const jsonCache = options.jsonCache || $d832f2ef8a5ce6ac$var$createJsonCache();
    const fetchImpl = options.fetchImpl || fetch.bind(globalThis);
    const tileSize = options.tileSize || $d832f2ef8a5ce6ac$var$DEFAULT_TILE_SIZE;
    const tileGap = options.tileGap || $d832f2ef8a5ce6ac$var$DEFAULT_TILE_GAP;
    return {
        backendBaseUrl: backendBaseUrl,
        buildManifestUrl: (bookUrl)=>$d832f2ef8a5ce6ac$var$buildManifestUrl(bookUrl, backendBaseUrl),
        getManifest: getManifest,
        getPageKey: getPageKey,
        fetchImageBlob: fetchImageBlob,
        renderPageToCanvas: renderPageToCanvas,
        renderPageToBlob: renderPageToBlob
    };
    async function getManifest(bookUrl, fetchOptions = {}) {
        const manifestUrl = $d832f2ef8a5ce6ac$var$buildManifestUrl(bookUrl, backendBaseUrl);
        const payload = await getCachedJson(manifestUrl, fetchOptions);
        return $d832f2ef8a5ce6ac$var$normalizeManifest(bookUrl, payload);
    }
    async function getPageKey(pageId, fetchOptions = {}) {
        const keyUrl = `${$d832f2ef8a5ce6ac$var$PAGE_KEY_ENDPOINT}${encodeURIComponent(pageId)}`;
        return getCachedJson(keyUrl, fetchOptions);
    }
    async function fetchImageBlob(imageUrl, fetchOptions = {}) {
        const response = await fetchImpl(imageUrl, {
            method: "GET",
            mode: "cors",
            cache: "force-cache",
            signal: fetchOptions.signal
        });
        if (!response.ok) throw new Error(`Image fetch failed with status ${response.status}`);
        return response.blob();
    }
    async function renderPageToCanvas(pageReference, fetchOptions = {}) {
        if (!pageReference?.pageId || !pageReference?.imgUrl) throw new Error("Page reference is missing pageId or imgUrl.");
        const [pageKey, imageBlob] = await Promise.all([
            getPageKey(pageReference.pageId, fetchOptions),
            fetchImageBlob(pageReference.imgUrl, fetchOptions)
        ]);
        return $d832f2ef8a5ce6ac$var$unscramblePage({
            imageBlob: imageBlob,
            pageKey: pageKey,
            tileGap: tileGap,
            tileSize: tileSize
        });
    }
    async function renderPageToBlob(pageReference, fetchOptions = {}) {
        const canvas = await renderPageToCanvas(pageReference, fetchOptions);
        const type = fetchOptions.type || "image/jpeg";
        const quality = fetchOptions.quality ?? 0.86;
        const blob = await $d832f2ef8a5ce6ac$var$canvasToBlob(canvas, type, quality);
        return {
            blob: blob,
            canvas: canvas,
            height: canvas.height,
            pageId: pageReference.pageId,
            width: canvas.width
        };
    }
    async function getCachedJson(url, fetchOptions = {}) {
        if (!fetchOptions.forceRefresh) {
            const cachedValue = await jsonCache.match(url);
            if (cachedValue) return cachedValue;
        }
        const response = await fetchImpl(url, {
            method: "GET",
            headers: {
                Accept: "application/json"
            },
            mode: "cors",
            signal: fetchOptions.signal
        });
        if (!response.ok) throw new Error(`Request failed with status ${response.status}`);
        const payload = await response.json();
        await jsonCache.put(url, payload);
        return payload;
    }
}
function $d832f2ef8a5ce6ac$var$buildManifestUrl(bookUrl, backendBaseUrl) {
    return `${backendBaseUrl}/api/rekhtaBook/${encodeURIComponent(bookUrl)}`;
}
function $d832f2ef8a5ce6ac$var$normalizeManifest(bookUrl, payload) {
    const data = payload?.data || payload;
    if (!data) throw new Error("Manifest payload is empty.");
    const pageIds = Array.isArray(data.pageIds) ? data.pageIds : [];
    const pages = Array.isArray(data.pages) ? data.pages : [];
    const scrambleMap = Array.isArray(data.scrambleMap) ? data.scrambleMap.map((item, index)=>({
            imageName: pages[index] || item.imgUrl?.split("/").pop() || "",
            imgUrl: item.imgUrl,
            index: index,
            keyUrl: item.key,
            pageId: pageIds[index] || $d832f2ef8a5ce6ac$var$extractPageId(item.key)
        })) : pageIds.map((pageId, index)=>({
            imageName: pages[index] || "",
            imgUrl: `https://ebooksapi.rekhta.org/images/${data._bookId}/${pages[index]}`,
            index: index,
            keyUrl: `${$d832f2ef8a5ce6ac$var$PAGE_KEY_ENDPOINT}${encodeURIComponent(pageId)}`,
            pageId: pageId
        }));
    return {
        actualUrl: data.actualUrl || "",
        author: data.author || "Unknown author",
        bookId: data._bookId || "",
        bookName: data.bookName || "Untitled book",
        bookUrl: bookUrl,
        fileName: data.fileName || "rekhta-book",
        pageCount: Number(data._pageCount) || scrambleMap.length || Math.max(pageIds.length, pages.length),
        pageIds: pageIds,
        pages: pages,
        scrambleMap: scrambleMap
    };
}
function $d832f2ef8a5ce6ac$var$extractPageId(url) {
    if (!url) return "";
    try {
        const parsedUrl = new URL(url);
        return parsedUrl.searchParams.get("pageid") || parsedUrl.searchParams.get("pgid") || "";
    } catch  {
        return "";
    }
}
async function $d832f2ef8a5ce6ac$var$unscramblePage(options) {
    const { imageBlob: imageBlob, pageKey: pageKey, tileGap: tileGap, tileSize: tileSize } = options;
    const source = await $d832f2ef8a5ce6ac$var$loadImageSource(imageBlob);
    const canvas = document.createElement("canvas");
    canvas.width = pageKey.PageWidth || tileSize * (pageKey.X || 1);
    canvas.height = pageKey.PageHeight || tileSize * (pageKey.Y || 1);
    const ctx = canvas.getContext("2d", {
        alpha: false
    });
    const tileStride = tileSize + tileGap;
    pageKey.Sub.forEach((sub)=>{
        ctx.drawImage(source, sub.X1 * tileStride, sub.Y1 * tileStride, tileSize, tileSize, sub.X2 * tileSize, sub.Y2 * tileSize, tileSize, tileSize);
    });
    $d832f2ef8a5ce6ac$var$releaseImageSource(source);
    return canvas;
}
async function $d832f2ef8a5ce6ac$var$loadImageSource(imageBlob) {
    if (typeof createImageBitmap === "function") return createImageBitmap(imageBlob);
    const objectUrl = URL.createObjectURL(imageBlob);
    try {
        const image = new Image();
        image.decoding = "async";
        const loaded = new Promise((resolve, reject)=>{
            image.onload = ()=>resolve(image);
            image.onerror = ()=>reject(new Error("Unable to decode page image."));
        });
        image.src = objectUrl;
        return await loaded;
    } finally{
        URL.revokeObjectURL(objectUrl);
    }
}
function $d832f2ef8a5ce6ac$var$releaseImageSource(source) {
    if (typeof ImageBitmap !== "undefined" && source instanceof ImageBitmap) source.close();
}
function $d832f2ef8a5ce6ac$var$canvasToBlob(canvas, type, quality) {
    return new Promise((resolve, reject)=>{
        canvas.toBlob((blob)=>{
            if (!blob) {
                reject(new Error("Unable to encode canvas output."));
                return;
            }
            resolve(blob);
        }, type, quality);
    });
}
function $d832f2ef8a5ce6ac$var$createJsonCache() {
    const cacheName = "rekhta-downloader-json-v1";
    return {
        async match (url) {
            const cacheStorage = await getCacheStorage();
            if (!cacheStorage) return $d832f2ef8a5ce6ac$var$memoryJsonCache.get(url) || null;
            const response = await cacheStorage.match(url);
            if (!response) return $d832f2ef8a5ce6ac$var$memoryJsonCache.get(url) || null;
            return response.json();
        },
        async put (url, payload) {
            $d832f2ef8a5ce6ac$var$memoryJsonCache.set(url, payload);
            const cacheStorage = await getCacheStorage();
            if (!cacheStorage) return;
            const response = new Response(JSON.stringify(payload), {
                headers: {
                    "Content-Type": "application/json"
                }
            });
            await cacheStorage.put(url, response);
        }
    };
    async function getCacheStorage() {
        if (!("caches" in globalThis)) return null;
        return caches.open(cacheName);
    }
}
function $d832f2ef8a5ce6ac$export$95265f97145e27cd(concurrency) {
    const queue = [];
    let activeCount = 0;
    return async (task)=>{
        if (activeCount >= concurrency) await new Promise((resolve)=>{
            queue.push(resolve);
        });
        activeCount += 1;
        try {
            return await task();
        } finally{
            activeCount -= 1;
            const nextTask = queue.shift();
            if (nextTask) nextTask();
        }
    };
}
function $d832f2ef8a5ce6ac$export$5bd7a6effa9290bc() {
    const hardwareConcurrency = navigator.hardwareConcurrency || 4;
    const deviceMemory = navigator.deviceMemory || 4;
    return {
        deviceMemory: deviceMemory,
        downloadConcurrency: Math.max(1, Math.min(2, Math.floor(Math.min(hardwareConcurrency, deviceMemory) / 2))),
        hardwareConcurrency: hardwareConcurrency,
        previewConcurrency: Math.max(1, Math.min(4, Math.floor((hardwareConcurrency + deviceMemory) / 3)))
    };
}


export {$d832f2ef8a5ce6ac$export$4c3438cd8db02a1e as DEFAULT_BACKEND_BASE_URL, $d832f2ef8a5ce6ac$export$b79684660ffced15 as createBookClient, $d832f2ef8a5ce6ac$export$95265f97145e27cd as createLimiter, $d832f2ef8a5ce6ac$export$5bd7a6effa9290bc as getDeviceProfile};
//# sourceMappingURL=index.js.map
